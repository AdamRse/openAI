<form method="post" action="{{ path('app_chat_session_delete', {'id': chat_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chat_session.id) }}">
    <button class="btn">Delete</button>
</form>
