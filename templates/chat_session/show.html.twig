{% extends 'base.html.twig' %}

{% block title %}ChatSession{% endblock %}

{% block body %}
    <h1>ChatSession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chat_session.id }}</td>
            </tr>
            <tr>
                <th>Ts</th>
                <td>{{ chat_session.ts ? chat_session.ts|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chat_session_index') }}">back to list</a>

    <a href="{{ path('app_chat_session_edit', {'id': chat_session.id}) }}">edit</a>

    {{ include('chat_session/_delete_form.html.twig') }}
{% endblock %}
