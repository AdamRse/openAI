<form method="post" action="{{ path('app_chat_version_delete', {'id': chat_version.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chat_version.id) }}">
    <button class="btn">Delete</button>
</form>
